BASE_OBJECTS = registration.o r_api.o cpu_features.o simd_dispatch.o
KERNEL_OBJECTS = @RSD_OBJECTS@
OBJECTS = $(BASE_OBJECTS)

PKG_CPPFLAGS = -I../inst/include
PKG_LIBS = $(KERNEL_OBJECTS)

all: $(SHLIB)

clean:
	rm -f $(SHLIB) $(BASE_OBJECTS) symbols.rds
