% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dispatch.R
\name{count_nonzero}
\alias{count_nonzero}
\title{Count non-zero bytes with the selected SIMD backend}
\usage{
count_nonzero(x)
}
\arguments{
\item{x}{A raw vector.}
}
\value{
A \code{double} scalar giving the count of non-zero bytes. The return
type is \code{double} rather than \code{integer} to accommodate vectors longer than
\code{.Machine$integer.max} without overflow.
}
\description{
Demonstration kernel for the runtime dispatch template. \code{count_nonzero()}
counts bytes that are not \code{00} in a raw vector using the currently selected
backend. The default backend is \code{"auto"}, which selects the best compiled
backend supported by the current CPU/runtime.
}
\examples{
count_nonzero(as.raw(c(0, 1, 0, 2)))
}
