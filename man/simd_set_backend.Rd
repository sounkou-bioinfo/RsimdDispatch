% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dispatch.R
\name{simd_set_backend}
\alias{simd_set_backend}
\title{Select the runtime SIMD backend}
\usage{
simd_set_backend(
  backend = c("auto", "scalar", "sse2", "sse41", "avx2", "avx512", "neon")
)
}
\arguments{
\item{backend}{One of \code{"auto"}, \code{"scalar"}, \code{"sse2"}, \code{"sse41"},
\code{"avx2"}, \code{"avx512"}, or \code{"neon"}.}
}
\value{
The selected backend, invisibly. For \code{"auto"}, this is the backend
chosen from the compiled and CPU-supported set.
}
\description{
Select the backend used by subsequent calls to \code{\link[=count_nonzero]{count_nonzero()}}. Unlike
loader-based systems that open one backend shared library per process,
\code{RsimdDispatch} keeps all compiled variants in one shared object and switches
guarded function pointers. This makes same-process benchmarking possible.
}
\examples{
old <- simd_backend()
simd_set_backend("scalar")
simd_set_backend("auto")
}
